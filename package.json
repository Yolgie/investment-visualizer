{
  "name": "retirement-calc",
  "version": "1.0.0",
  "description": "Browser-only investment & retirement calculator with a simplified Austrian tax model (KESt). Dev tooling only — the site is static files with no build step.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node test.js",
    "lint": "eslint .",
    "sync-vendor": "node -e \"const fs=require('fs');fs.mkdirSync('vendor',{recursive:true});for(const f of ['chart.umd.js','chart.umd.js.map'])fs.copyFileSync('node_modules/chart.js/dist/'+f,'vendor/'+f)\""
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.1"
  },
  "dependencies": {
    "chart.js": "4.4.7"
  }
}
